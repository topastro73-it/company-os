# vault/ — zona 🔴 RESTRICTED (solo admin)

Dati riservati: finance (bilanci, cap-table, cashflow, scadenzario), legal e contratti firmati.
Snapshot delle zone Drive `40-Finance` e `70-Contratti-Riservati` (`snapshot_target: vault`).

**Candidata a repo privato separato** (git submodule) se il repo di sistema è condiviso: così i
collaboratori clonano il sistema senza vedere il vault. Nel template è vuota.
Il secret-scan ammette IBAN e file 🔴 (cap-table, *-signed-*) SOLO qui dentro.
